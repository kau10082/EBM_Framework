# ── 機敏／本機專屬：永不進公開 repo ──────────────────
# 任何本機秘密檔（無 API key 也保留此忽略以防萬一）
.env

# 文獻原文（版權）＋抽取內容（可能含 PHI／病例資料）一律本機留存
inputs/*
cache/*
outputs/*
!inputs/.gitkeep
!cache/.gitkeep
!outputs/.gitkeep

# 封存的歷次分析（含衍生分析與原文引用片段）— 本機留存
runs/

# 執行期狀態（run_state.py 寫入；含本機絕對路徑/階段，per-run 不共享）
run_state.json

# ── 一般忽略 ─────────────────────────────────────────
__pycache__/
*.pyc
.venv/
venv/
.DS_Store
_strip*.py
# 移除 _build*.py：會誤殺核心渲染器 _build_pdf.py（GRADE PDF）；session 暫存由 _run_tmp/ 覆蓋
